/**
 * 피드 순서를 정하는 seed.
 *
 * 방문(브라우징 세션)마다 새로 뽑되 그 안에서는 유지한다 — maxAge 없는
 * 세션 쿠키인 이유다. 요청 사이에서 살아 있지 않으면 다음 페이지가 첫
 * 페이지와 겹치고(get_feed_v2는 seed가 같아야 순서가 재현된다), 새로고침
 * 할 때마다 피드가 통째로 재배열돼 "있던 게시물이 사라진" 것처럼 보인다.
 *
 * 서버 컴포넌트는 쿠키를 쓸 수 없어 미들웨어에서 심는다.
 */
export const FEED_SEED_COOKIE = "ttokttok.feed-seed";
