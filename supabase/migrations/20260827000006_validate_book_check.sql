-- ============================================================
-- books_needs_epub_or_store_ref 전수 검증
--
-- v2 마이그레이션 시점엔 시드 8권이 EPUB 없이 있어 NOT VALID로 걸어 두었다.
-- 위키문헌 수급으로 8권 모두 epub_path가 채워졌으므로 이제 기존 행까지
-- 검증한다. 이 명령은 위반 행이 하나라도 있으면 실패한다 — 통과하면
-- "본문도 서점 링크도 없는 도서"가 DB에 없다는 뜻이다.
-- ============================================================

alter table public.books validate constraint books_needs_epub_or_store_ref;
